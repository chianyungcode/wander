{
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "db:migrations": "bun run prisma migrate dev --name dev",
    "start:container": "bun run start && bun run db:migrations"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "@prisma/client": "^5.15.0",
    "hono": "^4.4.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prisma": "^5.15.0"
  }
}
